/**
 * Enum representing different sectors that organizations and sponsors can work in
 * Used for both organization sectors and sponsor interest areas
 */
export enum Sector {
  EDUCATION = 'Education',
  HEALTH = 'Health',
  TECHNOLOGY = 'Technology',
  AGRICULTURE = 'Agriculture',
  ENVIRONMENT = 'Environment',
  OTHERS = 'Others'
}