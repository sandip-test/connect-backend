/**
 * Enum representing different role that organizations and sponsors can work in
 * Used for both organization sectors and sponsor interest areas
 */
export enum Role {
  USER = 'user', // Just for any role that needed in future
  ORGANIZATION = 'organization',
  SPONSOR = 'sponsor',
  ADMIN = 'admin',
}
