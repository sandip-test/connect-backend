/**
 * @enum {string}
 * @description Defines the sectors of work.
 */

export enum Sectors {
  Education = 'Education',
  Health = 'Health',
  Technology = 'Technology',
  Agriculture = 'Agriculture',
  Environment = 'Environment',
}